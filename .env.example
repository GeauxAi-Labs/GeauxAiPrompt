# ─────────────────────────────────────────────
#  GeauxAiPrompt — Environment Configuration
#  Copy this file to .env and fill in your values.
#  NEVER commit .env to GitHub.
# ─────────────────────────────────────────────

# Your MentraOS developer API key (required)
MENTRA_API_KEY=your_mentra_api_key_here

# Your MentraOS account email (required)
# This must match the email you use to log into the Mentra app
OWNER_EMAIL=your@email.com

# MentraOS app package name (must match your app registration)
PACKAGE_NAME=com.yourname.yourapp

# AI provider: ollama | openai | anthropic (default: ollama)
AI_PROVIDER=ollama

# Model name — depends on provider
# ollama:    llama3.2, mistral, etc.
# openai:    gpt-4o-mini, gpt-4o, etc.
# anthropic: claude-3-haiku-20240307, etc.
AI_MODEL=llama3.2

# Ollama host (only needed when AI_PROVIDER=ollama)
OLLAMA_HOST=http://localhost:11434

# OpenAI API key (only needed when AI_PROVIDER=openai)
OPENAI_API_KEY=

# Anthropic API key (only needed when AI_PROVIDER=anthropic)
ANTHROPIC_API_KEY=

# Server port (default: 3000)
PORT=3000

# Milliseconds to pause between AI response pages on glasses (default: 5000)
PAGE_DELAY_MS=5000
